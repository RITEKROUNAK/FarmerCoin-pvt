"use strict";

module.exports = (plugin) => {
    // console.log(plugin.contentTypes)
    // console.log(plugin.contentTypes.user)
    // const lifecycles = require('./server/content-types/user/lifecycles')
    // plugin.contentTypes.user.lifecycles = lifecycles
    // console.log(plugin.contentTypes.user)
    // console.log(users)
    return plugin;
}

// module.exports = require('./server');